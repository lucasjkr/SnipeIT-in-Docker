This directory should .key and .pem file for Nginx:
    nginx-key.key
    nginx-key.pem

They can be created with the `generate-ssl-keys.sh` script in the scripts directory,
or real keys can be renamed and copied into that directory.