This file is a stub to create the sql/ directory.

The directory is 100% optional.

The MariaDB container will import any .sql files found within this directory during its build process
(it's linked to /docker-entrypoint-initdb.d/)

If you happen to want to run Snipes migrations separately from the installation, you could do so
and copy the resulting SQL Dump to this directory.

For example:
Doing so allows you to skip the setup process, set the default admin user, and import your locations list